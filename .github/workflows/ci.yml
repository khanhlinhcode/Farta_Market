name: Storefront CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: storefront-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout storefront
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Run unit tests
        run: npm test

      - name: Build
        run: npm run build

  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout storefront
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          path: websivi
          persist-credentials: false

      - name: Checkout backend integration dependency
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          repository: khanhlinhcode/BE_Farta-Market-
          ref: 6d8a63fdda051a8b13331bc87f6b2afba36985bf
          path: SVC01072023BE/SVC01072023BE
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: websivi/package-lock.json

      - name: Setup PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2
        with:
          php-version: "8.4"
          extensions: mbstring, pdo_sqlite, sqlite3
          coverage: none

      - name: Install backend dependencies
        run: composer install --no-interaction --prefer-dist --no-progress
        working-directory: SVC01072023BE/SVC01072023BE

      - name: Install storefront dependencies
        run: npm ci
        working-directory: websivi

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium
        working-directory: websivi

      - name: Run Playwright E2E
        run: npm run test:e2e
        working-directory: websivi

      - name: Upload failed Playwright traces
        if: failure()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: storefront-playwright-traces
          path: websivi/test-results/**/trace.zip
          if-no-files-found: ignore
          retention-days: 3
